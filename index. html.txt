<!DOCTYPE html>
<html lang="ta">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FF Store | Ultra Premium</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <style>
        :root {
            --primary: #00d2ff;
            --secondary: #00ff88;
            --accent: #ff007a;
            --dark: #05070a;
            --glass: rgba(255, 255, 255, 0.05);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Rajdhani', sans-serif; }
        
        body { 
            background: var(--dark);
            background-image: radial-gradient(circle at 50% 0%, #1e293b 0%, #05070a 100%);
            color: white; 
            min-height: 100vh;
            padding-bottom: 110px;
        }

        header { 
            padding: 20px; text-align: center; background: rgba(5, 7, 10, 0.8);
            backdrop-filter: blur(15px); border-bottom: 2px solid var(--primary);
            position: sticky; top: 0; z-index: 1000;
        }
        .logo { font-family: 'Orbitron', sans-serif; font-size: 1.5rem; font-weight: 800; color: var(--primary); text-shadow: 0 0 10px var(--primary); text-transform: uppercase; }

        /* Search Glow */
        .search-container { padding: 15px 20px; position: sticky; top: 70px; background: var(--dark); z-index: 999; }
        .search-box { 
            display: flex; background: var(--glass); border: 1px solid rgba(255,255,255,0.1); 
            border-radius: 12px; padding: 10px 15px; align-items: center;
            transition: 0.3s;
        }
        .search-box:focus-within {
            border-color: var(--primary);
            box-shadow: 0 0 15px rgba(0, 210, 255, 0.5);
        }
        .search-box input { background: transparent; border: none; color: white; margin-left: 10px; width: 100%; font-size: 1rem; }
        .search-box input:focus { outline: none; }

        .trust-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 10px; margin: 20px 0; text-align: center; }
        .trust-item i { color: var(--secondary); font-size: 1.8rem; margin-bottom: 8px; text-shadow: 0 0 10px var(--secondary); }
        .trust-item p { font-size: 0.75rem; color: #94a3b8; }

        .filter-scroll { display: flex; gap: 10px; overflow-x: auto; padding: 15px 0; scrollbar-width: none; }
        .filter-chip { background: var(--glass); padding: 6px 15px; border-radius: 20px; white-space: nowrap; font-size: 0.8rem; border: 1px solid rgba(255,255,255,0.1); cursor: pointer; }
        .filter-chip.active { background: var(--primary); color: black; font-weight: bold; }

        .page { display: none; padding: 20px; animation: fadeIn 0.4s ease-out; }
        .active-page { display: block; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .id-card { 
            background: var(--glass); border-radius: 20px; padding: 15px; margin-bottom: 20px; 
            border: 1px solid rgba(255,255,255,0.05);
        }

        /* Swiper with Arrows */
        .swiper { width: 100%; height: 180px; border-radius: 15px; margin-bottom: 12px; }
        .swiper-slide { background: #161b22; display: flex; align-items: center; justify-content: center; overflow: hidden; }
        .swiper-slide img { width: 100%; height: 100%; object-fit: cover; }
        .swiper-button-next, .swiper-button-prev { color: var(--primary) !important; transform: scale(0.6); }

        .price-row { display: flex; justify-content: space-between; align-items: center; margin-top: 15px; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 10px; }
        .price { color: var(--secondary); font-size: 1.5rem; font-weight: bold; }

        .premium-form input, .premium-form textarea { width: 100%; padding: 15px; margin-top: 10px; margin-bottom: 15px; background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.1); border-radius: 12px; color: white; }
        .file-upload-btn { background: #1a1f26; border: 1px dashed var(--primary); padding: 15px; border-radius: 12px; text-align: center; cursor: pointer; color: var(--primary); }

        .btn { width: 100%; padding: 15px; border-radius: 12px; border: none; font-weight: bold; text-transform: uppercase; cursor: pointer; transition: 0.3s; }
        .btn-buy { background: var(--primary); color: black; box-shadow: 0 5px 15px rgba(0, 210, 255, 0.3); }
        .btn-sell { background: var(--secondary); color: black; }

        .bottom-nav { position: fixed; bottom: 20px; left: 20px; right: 20px; background: rgba(10, 15, 25, 0.9); backdrop-filter: blur(20px); display: flex; justify-content: space-around; padding: 12px; border-radius: 20px; border: 1px solid rgba(255,255,255,0.1); z-index: 1001; }
        .nav-item { color: #64748b; text-align: center; font-size: 0.75rem; cursor: pointer; }
        .nav-item i { font-size: 1.4rem; display: block; margin-bottom: 4px; }
        .nav-item.active { color: var(--primary); }
    </style>
</head>
<body>

    <header>
        <div class="logo">FF STORE PRO</div>
    </header>

    <div id="search-area" class="search-container" style="display: none;">
        <div class="search-box">
            <i class="fas fa-search"></i>
            <input type="text" id="idSearch" placeholder="Search ID, Evo, Bundles..." onkeyup="filterIDs()">
        </div>
        <div class="filter-scroll">
            <div class="filter-chip active" onclick="filterCategory('all', this)">All IDs</div>
            <div class="filter-chip" onclick="filterCategory('evo', this)">Evo Guns</div>
            <div class="filter-chip" onclick="filterCategory('rare', this)">Rare Bundles</div>
        </div>
    </div>

    <div id="home" class="page active-page">
        <div class="id-card" style="background: linear-gradient(to bottom right, rgba(0,210,255,0.1), transparent);">
            <h2 style="color: var(--primary);">Premium Experience</h2>
            <p>The Most Trusted FF Marketplace</p>
            <div class="price-row">
                <span class="price">Verified ✅</span>
                <button class="btn btn-buy" style="width: auto; padding: 10px 20px;" onclick="showPage('buy', document.getElementById('nav-buy'))">Visit Store</button>
            </div>
        </div>

        <div class="trust-grid">
            <div class="trust-item">
                <i class="fas fa-shield-alt"></i>
                <p>Secure<br>Payment</p>
            </div>
            <div class="trust-item">
                <i class="fas fa-bolt"></i>
                <p>Fast<br>Delivery</p>
            </div>
            <div class="trust-item">
                <i class="fas fa-user-check"></i>
                <p>Verified<br>Sellers</p>
            </div>
        </div>
        <p style="text-align: center; color: #94a3b8; font-size: 0.8rem;">எங்கள் தளம் 100% நம்பகமானது. உங்கள் ஐடி பாதுகாப்பாக கைமாற்றப்படும்.</p>
    </div>

    <div id="buy" class="page">
        <div id="id-list">
            <div class="id-card" data-category="evo" data-title="Cobra Max">
                <div class="swiper mySwiper">
                    <div class="swiper-wrapper">
                        <div class="swiper-slide">
                            <img src="IMG-20260418-WA0000.jpg" alt="FF ID">
                        </div>
                        <div class="swiper-slide">
                           <p style="color:gray">Second Image Place</p>
                        </div>
                    </div>
                    <div class="swiper-button-next"></div>
                    <div class="swiper-button-prev"></div>
                </div>
                <h3>Cobra MP40 Max ID</h3>
                <p>UID: 1029XXXXXX | Level: 72</p>
                <div class="price-row">
                    <span class="price">₹ 4,500</span>
                    <button class="btn btn-buy" style="width: auto; padding: 8px 15px;">Buy</button>
                </div>
            </div>
        </div>
    </div>

    <div id="sell" class="page">
        <h2 style="margin-bottom: 20px; color: var(--secondary);">Sell Your ID</h2>
        <div class="premium-form">
            <input type="text" id="s_name" placeholder="Name">
            <input type="number" id="s_phone" placeholder="WhatsApp Number">
            <input type="text" id="s_uid" placeholder="UID & Level">
            <textarea id="s_desc" rows="4" placeholder="ID Highlights"></textarea>
            <input type="number" id="s_price" placeholder="Price (₹)">
            <div class="file-upload-btn" onclick="document.getElementById('file-input').click()">
                <i class="fas fa-image"></i> Choose ID Images
            </div>
            <input type="file" id="file-input" style="display: none;" multiple>
            <button class="btn btn-sell" style="margin-top:10px;" onclick="submitSell()">Submit Request</button>
        </div>
    </div>

    <div class="bottom-nav">
        <div class="nav-item active" onclick="showPage('home', this)">
            <i class="fas fa-home"></i>Home
        </div>
        <div id="nav-buy" class="nav-item" onclick="showPage('buy', this)">
            <i class="fas fa-shopping-bag"></i>Store
        </div>
        <div class="nav-item" onclick="showPage('sell', this)">
            <i class="fas fa-plus-circle"></i>Sell
        </div>
        <a href="https://wa.me/91YOURNUMBER" class="nav-item" style="text-decoration:none; color: #25d366;">
            <i class="fab fa-whatsapp"></i>Contact
        </a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script>
        var swiper = new Swiper(".mySwiper", {
            navigation: {
                nextEl: ".swiper-button-next",
                prevEl: ".swiper-button-prev",
            },
        });

        function showPage(pageId, element) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active-page'));
            document.getElementById(pageId).classList.add('active-page');
            document.getElementById('search-area').style.display = (pageId === 'buy') ? 'block' : 'none';
            document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
            if(element) element.classList.add('active');
            window.scrollTo(0,0);
        }

        function filterIDs() {
            let input = document.getElementById('idSearch').value.toLowerCase();
            let cards = document.querySelectorAll('#id-list .id-card');
            cards.forEach(card => {
                let title = card.getAttribute('data-title').toLowerCase();
                card.style.display = title.includes(input) ? "block" : "none";
            });
        }

        function filterCategory(cat, element) {
            document.querySelectorAll('.filter-chip').forEach(c => c.classList.remove('active'));
            element.classList.add('active');
            let cards = document.querySelectorAll('#id-list .id-card');
            cards.forEach(card => {
                let cardCat = card.getAttribute('data-category');
                card.style.display = (cat === 'all' || cardCat === cat) ? "block" : "none";
            });
        }

        function submitSell() {
            let n = document.getElementById('s_name').value;
            let a = document.getElementById('s_price').value;
            if(!n || !a) { alert("Details fill pannunga!"); return; }
            window.open(`https://wa.me/91YOURNUMBER?text=*SALE REQUEST*%0A*Name:* ${n}%0A*Price:* ₹${a}`, '_blank');
        }
    </script>
</body>
</html>

